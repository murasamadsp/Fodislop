#nullable enable

namespace Fodinae.Tools.Imgui;

/// <summary>
/// Что рисовать гизмо в мире.
/// </summary>
/// <remarks>
/// Отдельный объект, а не поля хозяина, потому что переключает их окно, а
/// рисует по ним <c>OnDrawGizmos</c> хозяина — двум разным местам нужно одно и
/// то же состояние.
///
/// Раньше эти флаги переключались клавишами 1, 2 и 3 (с дублями на нумпаде),
/// нигде не описанными. Клавиши убраны: цифровой ряд занят хотбаром инвентаря,
/// и отладка, отбирающая цифры у игры, — плохая отладка. Третий флаг,
/// «сущности», удалён: он переключался, но в отрисовку не передавался никогда.
/// </remarks>
public sealed class WorldGizmoOptions
{
    public bool ShowGrid { get; set; }

    public bool ShowCursor { get; set; }
}
